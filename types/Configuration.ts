export default interface Configuration {
    token: string;
    botId: string;
    guildId: string;
    canaryToken: string;
    canaryBotId: string;
    developers: string[];
    versionLogging: string;
    mongoose: string;
}
